<h1>List Students Eligible to Test</h1>

<p style="color: red"><%= flash[:error] %></p>

<% form_tag("/reports/test_eligibility", :method=>'post') do %>
	<%= label_tag("Enter Test Date:") %>
  	<%= text_field_tag("test_date") %> (YYYY-DD-MM)<br />
	<%= label_tag("Choose school:") %>
	<select name="school">
		<option > </option>
		<% for school in @schools %>
			<option value='<%= school.id %>' ><%= school.name %></option>
		<% end %>
	</select><br />
	<%= label_tag("Less required classes:")%>
	<%= text_field_tag("fudge_factor") %> <br />
	<%= submit_tag("List Eligible Students") %>
<% end %>

<p><%= flash[:notice] %></p>

<% for student in @students_eligible_to_test %>	
	<%= student.last_name%>, <%= student.first_name%></br>
<% end %>




