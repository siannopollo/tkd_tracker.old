  <%=  label_tag("First name") %>:
  <%=  label_tag(@student.first_name) %><br />
  
  <%=  label_tag("Last name") %>:
  <%=  label_tag(@student.last_name) %><br />
  
  <%= label_tag("School") %>:
  <%= label_tag(School.find(:first, :conditions => "id = '#{@student.school_id}'").name) %><br />
  
  <%=  label_tag("Rank") %>:
  <%=  label_tag(@student.rank) %><br />
  
  <%=  label_tag("last_test")%>:
  <%=  label_tag(@student.last_test)%><br />
  
  <%=  label_tag("Email Address")%>:
  <%=  label_tag(@student.email_address)%><br />
  
  <%=  label_tag("Phone")%>:
  <%=  label_tag(@student.phone)%><br />
  
<% if @student.attendances.length > 0 then%>
<h2>Attendances</h2>
  <table>
  	  <tr>
  	  	<td><b>Date</b></td>
		<td><b>Number of Classes</b></td>
  	  </tr>
	  <% for attendance in @student.attendances%>
	  	<tr style="background-color:<%=cycle('lightblue', 'white')%>">
	  		<td><%= attendance.created_at.strftime("%Y-%b-%d") %></td>
			<td><%= attendance.number_of_classes%></td>
	  	</tr>
	  <% end %>
  </table>
<% end %>
  
<%= link_to 'Edit', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %>
